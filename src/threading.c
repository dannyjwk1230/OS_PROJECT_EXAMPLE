#include "threading.h"

MiniOsStatus thread_run_tasks(ThreadTask *tasks, void **args, int task_count)
{
    /* TODO: 여러 작업을 스레드로 실행하고 모든 작업이 끝날 때까지 기다린다. */
    (void)tasks;
    (void)args;
    (void)task_count;
    return MINI_OS_NOT_IMPLEMENTED;
}
